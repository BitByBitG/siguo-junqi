# 2.5.11

- 首次启动一次性清空对局及房间聊天历史；在存档目录写入 reset-2.5.11 标记，之后启动不再清空，不再回退恢复旧路径历史。账号、Rating、头像、分组及首页聊天不变。
- 存档使用独立临时文件、fsync 和原子替换，损坏主文件不会覆盖有效备份。保留 2.5.10 的有战损不能悔棋修复。

# 2.5.10

- 有任何棋子死亡的走子不能悔棋；服务器校验同时覆盖旧存档中的悔棋快照，前端自动隐藏按钮。无战损走子仍按原条件撤回。

# 2.5.9

- 分组聊天共用首页十行折叠，保留展开状态，避免轮询和扣表情重建正文；增加放大聊天。
- 后台聊天管理可选择首页或分组，支持单条/批量删除、禁言及解除禁言，支持管理员密钥。
- 后台聊天共用 Markdown、LaTeX、图片及折叠显示，修整管理按钮尺寸。

# 2.5.8

- 修复加密通知解密期间连接断开导致退出、账号删除等通知丢失的问题。

- 移除 BOT 五步无战损判负，改为含 BOT 对局全桌连续 16 步无战损或总步数达到开局人数 × 128 自动和棋，不计 Rating。
- 保存无战损计数与开局人数，支持悔棋、重启，参考程序移除五步强制进攻策略；单步五秒超时规则不变。
- 关闭房间向在线成员发送退出通知，按原设置保留或清除历史。保留 Rated 对局进行中不能换人、普通房主不能关闭的规则。
- 移除旧查询参数房间入口，比赛列表改为 DOM 文本渲染。
- 修正过期测试断言、大厅认证等待与测试数据路径隔离，新增自动和棋边界测试。同步 README、规则、API 及对应网页。公告条数、安装方式不变。

# 2.5.7

- 修复管理页“分组管理 → 刷新”实际请求错接口的问题：管理页此前调用普通 `/api/groups`，该接口要求 `authorizeRequest()` 返回 username；仅使用管理密钥时返回的是 `{admin:true}`，因此刷新会失败。
- 新增只返回分组管理配置的 `/api/admin/groups`，同时支持管理员登录态和管理密钥；管理页刷新改用此接口。
- 刷新按钮旁显示“刷新中 / 已刷新 / 刷新失败”，便于确认请求是否真正完成。

# 2.5.6

- 补齐分组页面 `<link rel="icon">` 和 `apple-touch-icon`，使用与首页一致的 `/favicon.png?v=240`「軍」图标。

# 2.5.5

- 不再通过额外 `emoji-picker.js` 给分组页接表情面板。
- 将首页原本的 `emojiGroups + emojis + openEmojiPicker/closeEmojiPicker` 实现恢复到 `app.js`，并把同一段实现原样放入 `groups.html`；两页各自直接使用同一份表情数据和相同面板逻辑。
- 保留分组 `chat-react` Socket 作用域和 Socket 登录认证修复。
- 组内成员列表继续显示头像、Rating、军衔和主页链接。

# 2.5.4

- 修复分组页面 Socket 从未执行 `lobby-auth` 的问题。加密 Socket 中间件会拦截未认证的 `chat-react`，这是 2.5.2/2.5.3 表情按钮看起来正常但实际无法扣表情的根因。
- 分组页连接 Socket 后先使用当前登录 token 完成认证，再复用首页 `chat-react`。
- 组内成员列表继续保留头像、Rating、军衔以及个人主页链接。

# 2.5.3

- 修复分组聊天表情反应：改为直接复用首页现有的 Socket.IO `chat-react` 通道和同一个表情选择器，不再走单独的 REST 反应实现。
- 服务端 `chat-react` 增加 `group:<id>` 作用域，权限、禁言和表情白名单检查与现有聊天一致。
- 组内成员列表恢复头像、Rating 和军衔展示；头像与用户名仍可点击进入个人主页。

# 2.5.2

- 分组聊天恢复表情反应，但不再使用 2.5.0 擅自加入的四个固定快捷表情。
- 分组聊天与首页聊天现在复用同一个完整表情选择器：分类、最近使用、已有反应计数、再次点击取消反应的行为一致。
- 服务端分组表情使用与首页相同的表情白名单校验。

# 2.5.1

- 撤回 2.5.0 未经要求加入的分组聊天表情反应功能。
- 所有聊天消息头中的头像和用户名链接到对应用户主页。
- 普通页面/管理页面列表中的用户名自动链接到用户主页。
- 聊天正文明确排除用户名自动链接，正文里的普通用户名保持纯文本。
- 保留 2.5.0 中用户明确要求的分组聊天 Markdown、LaTeX、图片、禁言、撤回以及组员列表功能。
- 恢复分组页原来的 4 秒刷新周期；去掉未经要求加入的刷新时间提示和组员额外头像/段位展示。

# 2.5.0

- 修复管理界面“刷新分组”无明显效果：相关接口和前端请求均禁用缓存，并显示刷新完成时间。
- 修复批量/单个修改成员分组后界面不立即更新。
- 分组聊天室改为复用主聊天的富文本渲染模块：Markdown、LaTeX、安全图片渲染与主聊天一致。
- 分组聊天室支持粘贴图片、表情反应、5 分钟内自行撤回。
- 管理员可在分组聊天室直接禁言/解除禁言、删除消息。
- 分组图床使用 `group:<id>` 权限域，只有该组成员和管理员可访问。
- 分组聊天框下方显示该组全部成员。
- 分组聊天及禁言列表继续持久化，每个分组最多保留最新 100 条消息。

# 2.4.8

- 每日创建房间上限与是否保留聊天历史改为分组级设置，默认 3 个/不保留。
- 管理员可多选成员并批量修改所属分组。
- 管理界面删除全部分页，账号与图床等列表一次显示全部内容。
- 保留 2.4.7 的持久化 JSON 损坏自动恢复机制。

# 2.4.7 更新

- 修复 `rooms.json` 损坏时服务器启动直接因 `JSON.parse` 崩溃的问题。
- 房间存档读取失败时自动把坏文件改名为 `.corrupt-时间戳` 保留，不再阻止服务启动。
- 自动依次尝试 `rooms.json`、`.tmp`、`.bak` 和旧存档恢复。
- 每次保存房间存档前保留上一份 `.bak`，降低异常关机或磁盘问题造成的存档损失。
- 图床索引 JSON 损坏时同样容错；索引损坏期间不会误删已有图片文件。

# 2.4.6 更新

- 和棋只需所有仍存活玩家同意，阵亡玩家不参与表决。
- 复盘隐藏本地棋子标记。
- 删除首页“当前房间”模块及其查询逻辑。
- 管理页聊天与图床支持多选、全选和批量删除；管理员删除直接消失，用户自行撤回仍显示“已撤回”。
- 新增分组系统与第二行导航入口；默认分组、独立组内聊天室、管理员成员分配。
- 分组可分别暂停聊天、下棋、图床。
- 每个成员可单独设置每日创建房间上限（默认 3）和房间历史保留（默认关闭）。
- 对局历史持久化总容量限制 1 GiB，超限优先清理最早结束的已结束对局。
- 大厅、房间、分组聊天室均最多保留最新 100 条消息。
- 公告全站公开，未登录也可查看，仅管理员可发布。
